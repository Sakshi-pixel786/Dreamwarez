# TODO - Dreamwarez layout match

- [ ] Implement ServicePage to visually match Home:
  - [x] Update `src/pages/ServicePage.jsx` markup to reuse the same hero/container/button patterns.
  - [x] Update `src/pages/servicePage.module.css` to mirror Home’s spacing/typography/card/grid styles.
- [ ] Keep shared layout components unchanged (`SiteHeader`, `SiteFooter`).
- [ ] Run dev server and visually verify `/` vs one or more service routes (e.g. `/erp/`).


